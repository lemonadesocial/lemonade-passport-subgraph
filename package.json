{
  "name": "lemonade-referral",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ lemonade-referral",
    "create-local": "graph create --node http://localhost:8020/ lemonade-referral",
    "remove-local": "graph remove --node http://localhost:8020/ lemonade-referral",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lemonade-referral",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.4",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
